# LangChain Testing Ground

## Environment

```commandline
pip install langchain
pip install python-dotenv
pip install openai
```

```commandline
pip install arxiv
pip install pymupdf
pip install pyttsx3
pip install SpeechRecognition
```